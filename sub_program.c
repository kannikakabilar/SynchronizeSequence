#include <stdio.h>
#include <stdlib.h>

int main(void) {
    /*some program that takes in the value of nth fibonacci sequence
    and performs further operations*/
    int num1, num2;
    scanf("%d", &num1);
    scanf("%d", &num2);
    printf("Printing 2 received integers: %d, %d", num1, num2);
    return 0;
}